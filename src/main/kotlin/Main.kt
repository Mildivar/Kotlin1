import Post.Attachments.Attachments
import Post.Attachments.Video
import Post.Attachments.VideoAttachment
import Post.Post
import Post.Comment

fun main() {
   /* val newPost = WallService
//    newPost.add(Post(2))
    newPost.add(Post(3))
//    newPost.update(Post(999, text = "nenene"))
    newPost.createComment(Comment(2,4))
    println(newPost)
    val att: Attachments = VideoAttachment(Video())
//    val att2: Attachments = VideoAttachment(Video(13))
    println(att.type)
//    println(att2)*/
}